# WebSocket Server

Simple Node.js WebSocket server using `ws`.

## Run locally

1. Install:
npm install

2. Start server:
npm start

3. Connect from browser or client to:
ws://localhost:10000

## Deploy on Render
- Node environment
- Start command: `npm start`
- Root Directory: `/` (default)
